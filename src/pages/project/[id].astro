---
export const prerender=false;
// export function getStaticPaths(){return []}
import Layout from "@/layouts/Layout.astro"
import App from "@/components/Project/App.tsx"

const {id} = Astro.params
---

<Layout>
  
<App client:only id={id}/>
</Layout>
